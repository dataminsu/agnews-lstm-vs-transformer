# AG News term project - shared dependencies (Python 3.11 venv).
# torchtext 0.18.0 is the final release; it pins torch==2.3.0 and needs numpy<2,
# so versions below are matched to install together cleanly.
#
# CPU install is the default. For an NVIDIA GPU, install torch from the CUDA
# index BEFORE the rest, e.g.:
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# (torchtext 0.18.0 works against either the CPU or cu121 build of torch 2.3.0.)

torch==2.3.0
torchtext==0.18.0
datasets>=2.19,<3
# huggingface_hub 1.x rejects the non-namespaced id "ag_news"; pin to the 0.x
# line that datasets 2.x targets so load_dataset("ag_news") works as in the brief.
huggingface_hub>=0.23,<1.0
numpy==1.26.4
scikit-learn>=1.4
matplotlib>=3.8
# For running the final-submission notebook (train_eval.ipynb):
ipykernel>=6.29
nbconvert>=7.16
